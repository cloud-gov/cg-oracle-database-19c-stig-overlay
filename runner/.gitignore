# local-only: corporate TLS-inspection CA staged for behind-proxy builds (never commit a corp CA)
runner/corp-ca.pem
# local validation output
out/
# built Go client binary — built from source in the image, never committed (#11)
oraquery/oraquery
